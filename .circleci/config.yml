version: 2
jobs:
    eb_deploy:
      docker:
        - image: circleci/python:3.6.8
      steps:
        - run:
           command: |
              sudo pip install awsebcli
              eb use SWAPDROP-staging
              eb deploy
workflows:
    version: 2
    staging_deploy:
        jobs:
            - eb_deploy:
                filters:
                    branches:
                      only:
                       - staging



